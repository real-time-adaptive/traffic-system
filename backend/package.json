{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo '🔄 Running database migrations...' && pnpm run db:push && echo '📦 Building for deployment...' && wrangler deploy --dry-run",
    "deploy": "echo '☁️  Deploying to Cloudflare Workers...' && wrangler deploy --minify",
    "deploy:quick": "wrangler deploy --minify",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.7",
    "nanoid": "^5.1.6",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "drizzle-kit": "^0.31.7",
    "tsx": "^4.21.0",
    "wrangler": "^4.4.0"
  }
}
